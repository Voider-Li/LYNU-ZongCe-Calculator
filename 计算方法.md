## 综测计算流程总结

整个综测计算是一个 **6 步流水线**，从教务系统导出的原始成绩出发，最终生成一张按综测成绩排序的备查表。

### 各步骤说明

**① 原始成绩表**（输入）

- 教务系统直接导出的格式：每行一个学生，每列一门课程
- 表头含课程编号、类别（必修/任选/限选）、学分
- 末尾几列是系统已算好的：总成绩、平均成绩、学分绩点、平均学分绩点
- 原始表里包含**所有课程**（含网课等不计入综测的科目），后续步骤需要筛选出参评科目

**② 量化处理**

- 两部分：**二课量化**（第二课堂加分）+ **基础管理量化**（按月扣分，如三月/四月/五月/六月）
- 大总分 = `100 + Σ(各月基础管理量化)`（基础管理量化通常为负数或0，即扣分项）
- 量化最终分 = `(二课量化 + 大总分) / 2`
- 按量化最终分排名（`RANK` 函数，降序）

**③ 成绩学分绩点处理**（核心计算）

- **成绩部分**：从原始成绩中筛出参评科目的分数

- **综合量化测评**：直接引用量化处理表的最终分

- 学分绩点部分

  ：逐科算绩点

  - 公式：`IF(成绩<60, 0, (成绩-50)/10 × 学分)`
  - 不及格（<60）的科目绩点为 0
  - 综合量化测评作为一门 3 学分的"科目"参与绩点计算

- **平均学分绩点（不含量化）** = `Σ各科绩点 / 13.5`（纯文化课总学分）

- **平均学分绩点（含量化）** = `Σ(各科绩点+量化绩点) / 16.5`（含量化3学分）

- 三种排名：综合成绩排名（含量化绩点）、量化成绩排名、学习成绩排名（不含量化绩点）

- ⚠️ 注意：排名用的是 `RANK(...,0)` 即**降序**（数值越大名次越靠前）

**④ 排名百分比计算**

- 把第③步的三个排名分别 ÷ 总人数，得到百分比
- 综合成绩百分比、量化成绩百分比、学习成绩百分比

**⑤ 备查表填写**（输出）

- 按综测成绩（平均学分绩点含量化）从高到低排序
- 每人一行，含：学号、姓名、学习成绩及名次百分比、量化成绩及名次百分比、综测成绩及名次百分比







关键计算公式

量化大总分 = 100 + Σ(各月基础管理量化)

量化最终分 = (二课量化 + 大总分) / 2

单科绩点 = IF(成绩<60, 0, (成绩-50)/10 × 学分)

平均学分绩点(不含量化) = Σ单科绩点 / 全科总绩点（不含量化3分）

平均学分绩点(含量化) = Σ(单科绩点+量化绩点) / 全科总绩点（含量化3分）